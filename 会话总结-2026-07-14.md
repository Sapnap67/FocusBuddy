# FocusBuddy 会话总结 — 2026-07-14 上午~下午

> 给下一段对话的上下文

---

## 项目概况

**FocusBuddy** = 摄像头AI面部检测 + 番茄钟 + ESP32像素宠物

### 目标
- 目标用户：学生/远程办公者
- 核心痛点：走神不自知、缺乏动力、不了解自己专注模式
- 当前赛道：学习专注（可套安全/环保/智慧城市赛道）

---

## 核心技术

| 层级 | 技术 |
|------|------|
| AI检测 | MediaPipe FaceMesh 468点（CDN加载） |
| 4种检测算法 | EAR闭眼(0.22阈值) + 低头(headRatio 0.38) + 转头(turnRatio 0.62) + 哈欠(MAR 0.6) |
| 帧管线 | Camera 工具类（不是手动send循环！经验教训） |
| 番茄钟 | 25分钟倒计时，单按钮切换开始/停止 |
| 通信 | ESP32 WiFi AP模式 (10.10.10.1) |
| 启动 | python -m http.server 8080（必须用HTTP服务器，不能file://） |

---

## 当前文件结构

```
FocusBuddy/（在 Desktop\Hack Harvard Stuff\）
├── index_new.html       ← 🎯 当前主力文件！检测+番茄钟+像素猫+ESP32同步
├── mvp.html             ← 旧版MVP（也能跑，但没番茄钟和猫）
├── full.html            ← 旧版（有bug，按钮不响应）
├── 启动.bat             ← 双击打开 index_new.html
├── 启动MVP.bat          ← 双击打开 mvp.html
├── 启动full.bat         ← 双击打开 full.html
├── README.md            ← 英文README
├── .gitignore
├── 项目规划.md           ← 完整项目文档
├── 接口约定.md           ← DOM ID + JS API约定
├── 参考-旧项目分析.md     ← 旧项目检测器分析
├── 更新日志.md
├── 给Ryan的提示词.md     ← Ryan的前端开发提示词
├── css/style.css
├── js/
│   ├── facemesh.js      ← v2.2（Camera工具类，4种检测）
│   ├── esp32.js         ← ESP32通信层（含sendTimer）
│   ├── app.js           ← 主控（配合index.html的模块化版）
│   ├── pomodoro.js      ← Ryan的番茄钟类
│   ├── stats.js         ← Ryan的统计+Chart.js图表
│   └── settings.js      ← Ryan的设置面板
├── esp32/FocusBuddy_Matrix/
│   └── FocusBuddy_Matrix.ino  ← ESP32固件（/pet /timer /alert /status）
└── 同步/
    ├── 龙虾同步_1~5.md
    ├── Ryan的ESP32任务.md
    └── 给Ryan的龙虾提示词_v2.md
```

---

## ⚠️ 关键经验教训

1. **检测代码必须用旧项目算法**：dist()要乘实际视频宽高、EAR要加800ms眨眼过滤、帧循环用Camera工具类
2. **模块化拆分容易出bug**：目前单文件 index_new.html 最稳定，多JS文件加载各种冲突
3. **浅色主题是Vickie要求的**：背景米白(#faf8f3)，卡片纯白，橙色(#ff6b35)强调色
4. **按钮别留旧的**：旧项目残留的 startBtn/stopBtn 会崩溃整个JS
5. **框架不要**：原生HTML+CSS+JS，不引入React/Vue等

---

## 当前 index_new.html 功能清单

- ✅ FaceMesh 4种检测（闭眼/低头/转头/哈欠）
- ✅ 3秒分心阈值（短暂转头不报警）
- ✅ 番茄钟 25:00 倒计时
- ✅ 单按钮切换：▶开始 ↔ ■停止
- ✅ 完成时蜂鸣声提醒
- ✅ 16×16像素猫（摄像头画面底部）
  - 专注=开心猫 | 走神=担心猫 | 离开=睡觉猫
- ✅ ESP32 番茄钟倒计时同步 (/timer?m=&s=)
- ✅ ESP32 宠物表情同步 (/pet?state=happy|worried|sleep)
- ✅ 浅色主题（米白+橙色）
- ✅ 无FaceMesh绿点（检测跑但不显示）

---

## GitHub 状态

- 仓库：https://github.com/Sapnap67/FocusBuddy
- Vickie账号：Sapnap67
- Ryan账号：RyanChen-DREAM
- 协作规则：Vickie直接push main | Ryan发PR → Vickie审
- PR #1（Ryan）：Chart.js图表+番茄钟改进 → ✅ 已合并
- PR #2（Ryan）：ESP32倒计时+sendTimer → ✅ 固件+esp32.js已合入，full.html改动跳过
- PR #3（Ryan）：可能有新PR，需要检查

---

## ESP32 信息

- 型号：Waveshare ESP32-S3-Matrix (8×8 NeoPixel)
- WiFi AP：FocusBuddy / 12345678 → IP 10.10.10.1
- LED引脚：GPIO 39
- 上传：USB CDC On Boot = Enabled，按住BOOT→按RESET→松RESET→松BOOT→点上传
- API端点：
  - GET /pet?state=happy|worried|angry|sleep|celebrate
  - GET /timer?m=25&s=00
  - GET /alert（3秒红色闪烁）
  - GET /status（JSON）

---

## 待办

1. ⬆️ 上传ESP32固件测试
2. 🔗 联调：index_new.html ↔ ESP32（需连上FocusBuddy WiFi）
3. 📱 测试各种检测场景灵敏度
4. 🎬 录演示视频素材
5. 📄 项目简介、架构图、PPT
6. 🔍 检查Ryan的PR #3（如果有）

---

## 启动命令

```bash
# 当前主力
双击 → 启动.bat

# 或者手动
cd "Desktop\Hack Harvard Stuff\FocusBuddy"
python -m http.server 8080
# → 打开 http://localhost:8080/index_new.html
```
