# FocusBuddy 接口约定 v1.0

> 两份代码最终合并到同一个 `index.html`，通过此文档约定的 ID/API 互不冲突。
> **改任何 ID 或 API 前，先在更新日志里通知对方！**

---

## DOM ID 约定

### 摄像头区 (Vickie)
| ID | 用途 |
|----|------|
| `#video` | 摄像头 video 元素 |
| `#overlay` | FaceMesh canvas 叠加层 |
| `#camera-select` | 摄像头下拉框 |
| `#vision-status` | 视觉状态文字 (专注中/走神/离开) |
| `#vision-status-dot` | 状态圆点 (🟢🟡🔴⚪) |
| `#face-angle-display` | 面部角度调试信息 |

### 番茄钟区 (Ryan)
| ID | 用途 |
|----|------|
| `#pomodoro-timer` | 倒计时大数字 |
| `#pomodoro-label` | 状态标签 (专注时间/休息时间) |
| `#btn-pomodoro-start` | 开始按钮 |
| `#btn-pomodoro-pause` | 暂停按钮 |
| `#btn-pomodoro-reset` | 重置按钮 |
| `#btn-pomodoro-skip` | 跳过按钮 |

### 统计区 (Ryan)
| ID | 用途 |
|----|------|
| `#stats-pomodoros` | 今日完成番茄数 |
| `#stats-focus-time` | 今日专注总时长 |
| `#stats-distractions` | 今日走神次数 |
| `#stats-focus-rate` | 专注率百分比 |
| `#chart-pie` | Chart.js 饼图 canvas |
| `#chart-bar` | Chart.js 柱状图 canvas |

### 设置区 (Ryan)
| ID | 用途 |
|----|------|
| `#settings-panel` | 设置模态面板 |
| `#btn-settings-open` | 打开设置按钮 |
| `#btn-settings-close` | 关闭设置按钮 |
| `#setting-focus-duration` | 专注时长滑块 |
| `#setting-break-duration` | 休息时长滑块 |
| `#setting-sensitivity` | 走神敏感度下拉 |
| `#setting-notifications` | 通知开关 checkbox |

### 宠物区 (Vickie)
| ID | 用途 |
|----|------|
| `#pet-area` | 宠物展示容器 |
| `#pet-status` | 宠物当前状态文字 |
| `#pet-animation` | 宠物动画区 (Vickie 用 canvas 画) |

---

## JS 全局 API 约定

### Vickie 暴露 (挂 `window.FocusBuddyVision`)

```js
window.FocusBuddyVision = {
  init(),                    // 初始化 FaceMesh + 摄像头
  start(),                   // 开始检测
  stop(),                    // 停止检测
  getStatus(),               // → 'loading'|'focused'|'distracted'|'away'|'idle'
  getFaceAngle(),            // → { yaw, pitch, roll, confidence }
  onStatusChange(callback),  // callback(status)
  setSensitivity(level),     // level: 'low'|'medium'|'high'
  isReady(),                 // → boolean
  destroy(),                 // 清理资源
}
```

### Ryan 暴露 (挂 `window.FocusBuddyUI`)

```js
window.FocusBuddyUI = {
  updateVisionStatus(status),   // 更新视觉状态指示器
  updateFaceAngle(angle),       // 更新角度显示
  showWarning(msg),             // 显示页面内警告
  showCelebration(),            // 番茄完成庆祝动画
  getSettings(),                // → { focusDuration, breakDuration, sensitivity, notifications }
  onSettingsChange(callback),   // callback(settings)
}
```

### 共享事件总线 (App 层)

```js
window.FocusBuddyEvents = new EventTarget();
// 事件列表:
// 'vision:status-changed'  { detail: { status } }
// 'pomodoro:state-changed'  { detail: { state, remaining } }
// 'pomodoro:completed'      { detail: {} }
// 'settings:changed'        { detail: { ...settings } }
// 'pet:update'              { detail: { expression } }
```

---

## CSS 类名约定

| 类名 | 元素 | 说明 |
|------|------|------|
| `.card` | 卡片容器 | 圆角、阴影、内边距 |
| `.card-header` | 卡片标题 | 粗体、图标 |
| `.status-dot` | 状态圆点 | 24px圆，`bg-*`控制颜色 |
| `.bg-focused` | 状态背景 | 绿色 #27ae60 |
| `.bg-distracted` | 状态背景 | 黄色 #f39c12 |
| `.bg-away` | 状态背景 | 红色 #e74c3c |
| `.bg-idle` | 状态背景 | 灰色 #95a5a6 |
| `.pixel-font` | 像素字体 | 等宽/custom |
| `.hidden` | 隐藏元素 | display:none |

---

## 文件归属

| 文件 | 负责人 | 说明 |
|------|--------|------|
| `index.html` | 共享 | 合并前各自用本地副本，合并时 Vickie 合并 |
| `css/style.css` | Ryan | 所有样式 |
| `js/facemesh.js` | Vickie | FaceMesh + 朝向算法 |
| `js/esp32.js` | Vickie | ESP32 WiFi 通信 |
| `js/pomodoro.js` | Ryan | 番茄钟逻辑 |
| `js/stats.js` | Ryan | 统计 + localStorage |
| `js/settings.js` | Ryan | 设置面板 |
| `js/app.js` | Vickie | 主控状态机 + 集成 |
| `esp32/FocusBuddy_Matrix/` | Vickie | ESP32 固件 |
| `接口约定.md` | 共享 | 本文件 |

---

## 合并检查清单

合并前确认:
- [ ] 所有 ID 与本文档一致
- [ ] Vickie 暴露了 `window.FocusBuddyVision`
- [ ] Ryan 暴露了 `window.FocusBuddyUI`
- [ ] CSS 类名无冲突
- [ ] CDN 引用一致（双方用同一个版本链接）
- [ ] 本地 `index.html` 合并为一个
