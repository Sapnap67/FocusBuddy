# FocusBuddy 更新日志

---

## 2026-07-14

### 15:20 — Ryan: 今日计划列表功能 📋
- ✅ 新增 `📋 今日计划` 卡片（`index_new.html`）
- ✅ 输入提示词（每行一条任务，支持 "2小时" / "30分钟" / "1h" 格式）→ 自动解析成待办列表
- ✅ 每项任务显示预计耗时（默认一个番茄钟 25min）
- ✅ Checkbox 标记完成，自动统计进度
- ✅ localStorage 持久化（"focusbuddy_plan_YYYY-MM-DD"），刷新后恢复
- ✅ Ctrl+Enter 快捷键生成
- ✅ 样式：暖色调 checkbox、完成项划线
- ⬆️ 待 PR

### 15:14 — Ryan: ESP32倒计时显示 + 冲突解决 + PR #2
- ✅ `FocusBuddy_Matrix.ino` 新增 `/timer` 端点，5×3 数字字体绘制倒计时，归零自动触发 celebrate
- ✅ `js/esp32.js` 新增 `sendTimer(min, sec)` 函数
- ✅ `js/app.js` `onTick` 每秒同步倒计时到 ESP32，`onComplete` 触发 celebrate + 归零
- ✅ `full.html` `esp32SendFocusData` 从桩改为真实 HTTP 调用（宠物表情 + 专注分数转倒计时）
- ✅ 与 Vickie 的 main branch rebase，解决 3 处冲突（`full.html` 的 esp32SendFocusData / 定时器间隔 / pomodoroResume）
- ✅ Bugfix: 冒号列位置 col=3→4；Celebrate 动画新增 5s 超时自动退出；Celebrate 计时器改用独立变量
- ⬆️ PR #2: `ryan/esp32-timer`

### 11:28 — Ryan: PR #1 合入 — 番茄钟skip + Charts + 音效 + CSS
- ✅ `js/pomodoro.js` skip() 支持 FOCUSING 跳过（跳到休息）和 ON_BREAK 跳过（回到 IDLE）
- ✅ `js/stats.js` 新增 `initCharts()` / `updateCharts()` — Chart.js 饼图+柱状图；`playSound()` — Web Audio API
- ✅ `js/app.js` 集成图表初始化和更新，番茄完成播放音效
- ✅ `css/style.css` 图表布局、跳过按钮样式、状态文字增强
- ⬆️ PR #1: `ryan/pomodoro-charts` → main ✅ merged

### 09:50 — MVP 网站上线 🎉
- ✅ 创建 `mvp.html`（自包含单文件，摄像头+FaceMesh+状态判定+计时）
- ✅ 创建 `启动MVP.bat`（双击启动本地服务器+自动打开浏览器）
- ✅ 创建 `同步/龙虾同步_1.md`（跨电脑信息同步机制启动）
- ⏳ 待办: 10:00 技术锁定 + 设备检查 + GitHub 仓库建立

### 09:45 — 参考旧项目
- ✅ 分析 `专注力检测器` 旧项目
- ✅ 发现可复用: EAR闭眼/低头/转头检测 + 专注度评分
- ✅ 写入 `参考-旧项目分析.md`

### 09:35 — 模块化骨架搭建完成 ⚡
- ✅ 比赛模式启动 + 龙虾同步技能已注册
- ✅ 创建 `接口约定.md`（DOM ID + JS API + CSS类名 + 文件归属 + 合并检查清单）
- ✅ 创建 `index.html`（骨架布局，所有ID已约定）
- ✅ 创建 `css/style.css`（基础样式 + CSS变量）
- ✅ 创建 `js/facemesh.js`（Vickie: FaceMesh+朝向算法骨架，API已暴露）
- ✅ 创建 `js/esp32.js`（Vickie: ESP32通信层，syncPet映射）
- ✅ 创建 `js/pomodoro.js`（Ryan: 番茄钟类，含状态机/回调）
- ✅ 创建 `js/stats.js`（Ryan: 统计+localStorage）
- ✅ 创建 `js/settings.js`（Ryan: 设置面板+持久化）
- ✅ 创建 `js/app.js`（Vickie: 主控状态机+集成，含FocusBuddyUI实现）
- ✅ 设置 10:00 闹钟提醒

### 09:30 — 项目启动 🚀
- ✅ 确定项目方向: 学习专注伴侣
- ✅ 创建 `项目规划.md`（目标用户/痛点/MVP/流程/分工/时间线）
- ✅ 创建 `给Ryan的提示词.md`（前端开发完整提示词）

---

> 格式: `时间 — 事件` · 每次改完代码记一条，commit 后标记 ⬆️
