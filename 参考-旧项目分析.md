# 参考：旧项目「专注力检测器」分析

> 来源: `HH/专注力检测器/`  
> 分析时间: 2026-07-14 09:45

---

## 技术栈

- MediaPipe FaceMesh (468关键点)
- 纯前端 HTML+CSS+JS，无框架
- DeepSeek API（AI报告功能）
- Web Audio API（提示音）
- localStorage风格的手动日志导出

---

## 检测算法（可直接复用）

### 1. EAR 闭眼检测 ⭐⭐⭐
```js
// 眼睛开合比例 < 0.22 判定闭眼
// 持续 >800ms 才算瞌睡（过滤正常眨眼）
LEFT_EYE  = [33, 160, 158, 133, 153, 144]
RIGHT_EYE = [362, 385, 387, 263, 373, 380]
```
**FocusBuddy可用**: 加上闭眼检测作为辅助判断（眼睛闭着肯定没在看屏幕）

### 2. 低头检测 ⭐⭐
```js
// noseToChin / faceHeight < 0.38 → 低头
faceHeight = dist(foreHead[10], chin[152])
headRatio = dist(noseTip[1], chin[152]) / faceHeight
```
**FocusBuddy可用**: 比pitch角度更稳定，不受面部大小影响

### 3. 东张西望检测 ⭐⭐
```js
// min(noseToLeft, noseToRight) / max(noseToLeft, noseToRight) < 0.62 → 转头
// 关键点: noseTip[1], leftCheek[234], rightCheek[454]
```
**FocusBuddy可用**: 更精确的yaw角替代方案

### 4. 打哈欠检测 ⭐
```js
// MAR > 0.6 且持续 >1200ms → 打哈欠
MAR = dist(mouthTop[13], mouthBottom[14]) / dist(mouthLeft[61], mouthRight[291])
```
**FocusBuddy可用**: 疲劳提醒功能

### 5. 专注度评分系统 ⭐⭐⭐
```js
// 分心时 -0.15/帧，专注时 +0.05/帧，范围0-100
focusScore = Math.max(0, focusScore - 0.15);
focusScore = Math.min(100, focusScore + 0.05);
```
**FocusBuddy可用**: 直接移植，比简单的是/否更有激励效果

### 6. 分心阈值计时器 ⭐⭐
```js
// 分心持续超过N秒才触发提醒（可配置1-10秒）
// 避免短暂转头就报警
```
**FocusBuddy可用**: 已实现类似逻辑，可参考其 slider 配置方式

---

## 可复用代码

| 功能 | 文件 | 复用度 |
|------|------|--------|
| EAR算法 + 眼部关键点索引 | index.html JS部分 | 可直接复制 |
| 低头检测(headRatio) | index.html | 可直接复制 |
| 转头检测(turnRatio) | index.html | 可直接复制 |
| 打哈欠检测(MAR) | index.html | 可选 |
| 专注度评分(增减逻辑) | index.html | 可直接复制 |
| Web Audio API beep | index.html | 可直接复制 |
| 暗色主题CSS | index.html `<style>` | 可参考配色 |
| status-badge动画 | index.html | 可参考 |
| 日志导出为txt | index.html | 可选 |

---

## 与FocusBuddy的差异

| | 旧项目 | FocusBuddy |
|------|--------|------------|
| 检测重点 | 分心（闭眼/低头/转头/哈欠） | 面部朝向屏幕判断 |
| 反馈方式 | 弹窗+声音 | LED宠物+Web仪表盘 |
| 时间管理 | 无 | 番茄钟 |
| 硬件 | 无 | ESP32 Matrix |
| 统计 | 基础计数 | localStorage持久化+Chart.js图表 |
| AI | DeepSeek报告 | 无（MVP不包含） |

---

## 建议复用优先级

1. **P0 直接移植**: EAR闭眼检测 + 专注度评分系统 → 加到 facemesh.js
2. **P1 参考改进**: 转头检测算法替代当前的简易yaw计算 → 更精确
3. **P2 可选增强**: 打哈欠疲劳检测、Web Audio提示音
4. **P3 不做**: DeepSeek AI报告（MVP不需要，以后可以加）

---

## 注意

- 旧项目有 `config.js` 含 API Key，⚠️ 不要复制到 FocusBuddy
- 旧项目需要本地服务器（Python HTTP），FocusBuddy 同样
- 旧项目没有模块化，所有代码在单个HTML；FocusBuddy已拆分成独立JS文件
